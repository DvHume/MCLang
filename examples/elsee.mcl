scoreboard set player_hp 100
scoreboard add player_hp -50

say {"Check..."}

# IF
# # Result: Player dead

execute if score player_hp matches 0 run say "Player dead"

# ELSE
# # Result: Player alive

execute if score player_hp matches 0 run say {"Player dead"} else run say {"Player alive"}